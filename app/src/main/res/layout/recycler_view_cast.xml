<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        style="@style/AppTheme.ImageSize"
        android:id="@+id/list_image_character"
        android:contentDescription="@string/cast" />

    <LinearLayout
        android:layout_toRightOf="@+id/list_image_character"
        android:layout_toEndOf="@+id/list_image_character"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerVertical="true">

        <TextView
            android:id="@+id/list_character_name"
            style="@style/AppTheme.Subtitle"
            android:singleLine="true" />

        <TextView
            android:id="@+id/list_cast_name"
            style="@style/AppTheme.Text"
            android:ellipsize="end"
            android:singleLine="true" />
    </LinearLayout>

</RelativeLayout>